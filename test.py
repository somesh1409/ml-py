# -*- coding: utf-8 -*-"""Spyder EditorThis is a temporary script file."""import pandas as pdimport numpy as npdata_train = pd.read_csv('kaggle-titanic/train.csv')data_test = pd.read_csv('kaggle-titanic/test.csv')data_train.replace({'male': 1, 'female': 0}, inplace=True)data_train = data_train[['Survived', 'Pclass', 'Sex', 'Age', 'SibSp', 'Parch', 'Fare']].dropna()data_test.replace({'male': 1, 'female': 0}, inplace=True)data_test = data_test.fillna(data_test['Age'].mean())data_test_2 = data_test[['Pclass', 'Sex', 'Age', 'SibSp', 'Parch', 'Fare', 'PassengerId']]data_test = data_test[['Pclass', 'Sex', 'Age', 'SibSp', 'Parch', 'Fare']]x_train = data_train[['Pclass', 'Sex', 'Age', 'SibSp', 'Parch', 'Fare']]y_train = data_train[['Survived']]from sklearn.preprocessing import StandardScalersc = StandardScaler()X_train = sc.fit_transform(x_train)X_test = sc.transform(data_test)from sklearn.naive_bayes import GaussianNBmodel = GaussianNB()model.fit(X_train, y_train)from sklearn import metricspredict_test = model.predict(X_test)solution = pd.DataFrame(data_test_2, columns = ['PassengerId'])solution['Survived']=predict_testsolution.to_csv('solution.csv', index=False)